websocket

indexing

go routine vs thread
new vs make

context

GMP model in Go

vertacila scalling vs horixal scalling

channel with new

mysql connection (tcp handshake ) connectinion pooling

indexing not used each column

race conditions vs deadlock

jwt


oop
polymorphsim

compostion 
struct embedding

pprof : profiling tool

skip analysis
reflect

enum

map, slice, array ,slice array across functions, slices with append, interface nil , context, reflect- mcq question which contain code and assume answer

batch = batch[:1] vs batch = append(batch[:1])
